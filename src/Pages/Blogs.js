import React from 'react';
import { useTitle } from '../routes/useTitle';

const Blogs = () => {
    useTitle("Blogs")
    return (
        <div>
            
        </div>
    );
};

export default Blogs;